"""Semantic Similarity (Bölüm 19).

STUB: Embedding tabanlı benzerlik (multilingual-e5 / LaBSE) Faz 3
kapsamında entegre edilecek (plan Bölüm 52). Faz 1'de gerçek bir model
olmadığı için bu fonksiyon None döner ve Quality Gate bu kontrolü
atlar; bu durum debug çıktısında açıkça işaretlenmelidir (bkz. engine.py
fallback_level).
"""
from typing import Optional


def cosine_similarity(original: str, humanized: str) -> Optional[float]:
    """Şu an implemente edilmedi. Faz 3'te embedding modeliyle doldurulacak."""
    return None
