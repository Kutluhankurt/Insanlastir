fastapi>=0.104,<1.0
uvicorn[standard]>=0.24,<1.0
pyyaml>=6.0,<7.0
pytest>=7.4,<8.0

# Bölüm 50: Türkçe morfolojik analiz (Zemberek'in saf Python portu).
# 0.1.3 Python 3.8'de `set[X]` (PEP 585) sözdiziminden dolayı çöküyor,
# bu yüzden 0.1.2'ye sabitlendi.
zeyrek==0.1.2
